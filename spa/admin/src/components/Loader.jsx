import React from 'react'

const Loader = () => {
    return (
        <div className='user-switcher_loader'>
            <img src={ USER_SWITCHER.asseturl + '/img/loader.gif'} />
        </div>
     );
}

export default Loader;